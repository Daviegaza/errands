import { motion } from "framer-motion";
import { ArrowLeft, Award, BadgeCheck, CalendarDays, CheckCircle2, Clock3, Flag, Heart, MessageCircle, PackageCheck, ShieldCheck, Star, Zap } from "lucide-react";
import { useNavigate, useParams } from "react-router-dom";
import { Avatar, Badge, Button, Card, IconButton, Progress, Rating } from "../../components/ui";
import { runners } from "../../data/mockData";
import { useAppStore } from "../../store/appStore";

const reviews=[{name:"Mercy W.",text:"Careful, quick and communicated at every step.",date:"2 days ago"},{name:"Daniel O.",text:"Very professional. The documents arrived ahead of schedule.",date:"Last week"},{name:"Faith K.",text:"Exactly the kind of reliable help I needed.",date:"2 weeks ago"}];
const trustBadges=[{icon:ShieldCheck,label:"Verified",detail:"Identity & phone"},{icon:Award,label:"Top Rated",detail:"4.9+ rating"},{icon:Zap,label:"Fast responder",detail:"Under 3 min"},{icon:PackageCheck,label:"300+ jobs",detail:"Experienced"}];
const taskMix=[{label:"Packages",value:78},{label:"Documents",value:56},{label:"Shopping",value:34}];

export function RunnerProfilePage(){const {id}=useParams();const nav=useNavigate();const {savedRunnerIds,toggleSavedRunner,selectRunner,addToast}=useAppStore();const runner=runners.find(r=>r.id===id)||runners[0];const saved=savedRunnerIds.includes(runner.id);const choose=()=>{selectRunner(runner);addToast(`${runner.name} selected.`);nav("/tracking/errand-1")};return <div className="profile-view"><header className="profile-cover"><IconButton label="Back" onClick={()=>nav(-1)}><ArrowLeft size={20}/></IconButton><div className="cover-actions"><IconButton label="Report runner" onClick={()=>addToast("Report form opened.","warning")}><Flag size={18}/></IconButton><IconButton label="Save runner" onClick={()=>toggleSavedRunner(runner.id)}><motion.span animate={saved?{scale:[1,1.3,1]}:{}} transition={{duration:.3}}><Heart size={18} fill={saved?"currentColor":"none"}/></motion.span></IconButton></div></header><motion.section className="runner-profile-hero" initial={{opacity:0,y:16}} animate={{opacity:1,y:0}} transition={{duration:.35,ease:"easeOut"}}><motion.div initial={{scale:.85,opacity:0}} animate={{scale:1,opacity:1}} transition={{type:"spring",stiffness:260,damping:20,delay:.05}}><Avatar initials={runner.initials} src={runner.photo} color={runner.color} size="xl" online/></motion.div><div><Badge tone="green"><BadgeCheck size={14}/> Identity verified</Badge><h1>{runner.name}</h1><p><Star size={17} fill="currentColor"/> {runner.rating} <span>· {runner.completed} completed errands</span></p></div><div className="profile-hero-actions"><Button variant="secondary" icon={<MessageCircle size={17}/>} onClick={()=>nav(`/messages/${runner.id}`)}>Message</Button><Button onClick={choose}>Choose runner</Button></div></motion.section><div className="runner-profile-grid"><main><motion.div initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.4}} transition={{duration:.3,ease:"easeOut"}}><Card className="profile-stats"><span><CheckCircle2/><b>{runner.reliability}%</b><small>Completion</small></span><span><Clock3/><b>{runner.responseTime}</b><small>Response time</small></span><span><CalendarDays/><b>{runner.memberSince}</b><small>Member since</small></span></Card></motion.div><motion.div initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.3}} transition={{delay:.06,duration:.3,ease:"easeOut"}}><Card className="profile-section"><h2>What {runner.name.split(" ")[0]} does best</h2><div className="specialty-list">{runner.specialties.map(item=><Badge key={item} tone="neutral">{item}</Badge>)}</div><div className="profile-progress"><Progress value={runner.reliability} label="Reliable completion"/><Progress value={96} label="On-time arrival"/></div></Card></motion.div><motion.div initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.2}} transition={{delay:.1,duration:.3,ease:"easeOut"}}><Card className="profile-section"><div className="section-heading"><div><p>Recent feedback</p><h2>What clients say</h2></div><Badge tone="green">Top 5%</Badge></div>{reviews.map((review,index)=><motion.div className="review-item" key={review.name} initial={{opacity:0,y:12}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.6}} transition={{delay:index*.08,duration:.25,ease:"easeOut"}}><Avatar initials={review.name.replace(".","")} size="sm"/><div><div><b>{review.name}</b><Rating value={5}/><small>{review.date}</small></div><p>{review.text}</p></div></motion.div>)}</Card></motion.div></main><aside><motion.div initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.3}} transition={{duration:.3,ease:"easeOut"}}><Card className="profile-section"><h2>Trust & recognition</h2><div className="badge-grid">{trustBadges.map((b,index)=><motion.span key={b.label} initial={{opacity:0,scale:.85}} whileInView={{opacity:1,scale:1}} viewport={{once:true,amount:.6}} transition={{delay:index*.06,duration:.25,ease:"easeOut"}}><b.icon/><b>{b.label}</b><small>{b.detail}</small></motion.span>)}</div></Card></motion.div><motion.div initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.3}} transition={{delay:.08,duration:.3,ease:"easeOut"}}><Card className="profile-section"><h2>Recent task mix</h2><div className="task-mix">{taskMix.map((task,index)=><span key={task.label}>{task.label} <motion.i initial={{width:0}} whileInView={{width:`${task.value}%`}} viewport={{once:true,amount:.6}} transition={{delay:index*.08,duration:.5,ease:"easeOut"}}/></span>)}</div></Card></motion.div><button className="report-link" onClick={()=>addToast("Report form opened.","warning")}><Flag size={16}/> Report or block runner</button></aside></div><div className="mobile-sticky-cta"><Button variant="secondary" onClick={()=>nav(`/messages/${runner.id}`)}>Message</Button><Button onClick={choose}>Choose runner</Button></div></div>}
